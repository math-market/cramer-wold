# Cramér–Wold in Lean — a Problem Market board

Prove that a probability distribution on `ℝⁿ` is determined by all its one-dimensional
projections. The locked statement is in [`CramerWold.lean`](CramerWold.lean); the task, win
conditions and how to submit are in [`TASK.md`](TASK.md).

```bash
lake exe cache get && lake build     # green, with exactly one `sorry` — that is the task
```
